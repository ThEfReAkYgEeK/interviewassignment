import 'package:flutter/material.dart';
import 'package:technosoftwares/planspage.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: PlansPage(),
    );
  }
}
